//! Bob CLI Agent — composition root
//!
//! Wires adapters into the runtime and exposes a CLI interface.

fn main() {
    // CLI agent is scaffolded but not yet wired.
    // Implementation: config loading → adapter wiring → REPL loop.
}
